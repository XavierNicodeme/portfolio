{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="test min-vh-100">
                <div class="block-draw d-flex justify-content-center">
                    <div class="entete me-3">
                        <h1 class="entete-text">bonjour, </h1>
                        <h2 class="entete-text d-flex">je suis <p class="text-primary ms-2 fw-bold mb-0"> Xavier Nicodème</p>,</h2>
                        <h2 class="entete-text">développeur junior full-stack</h2>
                    </div>
                    <img src="{{ asset('build/images/drawbedy.png') }}" class="draw" alt="Draw Profile">
                </div>
            </div>
            <div id="projets" class="projets min-wh-100 mb-5 min-vh-100">
                <h2 class="projets-section text-center my-5 text-dark">.mes projets</h2>
                <div class="d-flex flex-wrap col-9 mx-auto justify-content-center mb-5">
                    {% for projet in projets %}
                        <div class="col-4 my-5">
                            <p class="ms-5 fw-bold text-dark">{{ projet.name }}</p>
                            <a href="{{ path('projet_show', {id: projet.id}) }}">
                                <img class="projet-img rounded" src="{{ asset('build/images/' ~  projet.image) }}" alt="{{ projet.name }}"/>
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div id="contact">
                <h2 class="contact-section text-center text-dark">.contact</h2>
                <div class="mt-5 col-6 mx-auto border p-4 mb-5">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-primary">Envoyer</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
